# Environment Configuration
# Copy this file to .env.local and fill in your values

# Node Environment
NODE_ENV=production

# Database Configuration (Choose one based on your deployment)

# For Vercel with PostgreSQL (recommended for production)
# DATABASE_TYPE=postgres
# DB_HOST=your-postgres-host.vercel.sh
# DB_PORT=5432
# DB_USER=postgres
# DB_PASSWORD=your_password
# DB_NAME=pos_system

# For SQLite (local development only)
DATABASE_TYPE=sqlite
DATABASE_PATH=./pos-database.sqlite

# JWT Configuration
JWT_SECRET=53404c6cf5795d120d312cf779b92dc0

# CORS Configuration
CORS_ORIGIN=*

# Server Configuration
HOST=0.0.0.0
PORT=3000

# Vercel Configuration
VERCEL=
VERCEL_ENV=development
